import { MainScreen } from "./MainScreen";

const FriendScreen = () => {
  return (
    <main className="w-full max-w-full flex flex-col flex-grow box-border overflow-hidden z-0 bg-bgScreen">
      <MainScreen />
      {/* <ChatBox /> */}
    </main>
  );
};

export default FriendScreen;
